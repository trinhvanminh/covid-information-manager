const userController = {
  login: async (req, res) => {
    try {
      // do it
    } catch (error) {
      // handle error
    }
  },
  register: async (req, res) => {
    try {
      // do it
    } catch (error) {
      // handle error
    }
  },
};

module.exports = userController;
